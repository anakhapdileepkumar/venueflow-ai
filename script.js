function findGate() {
  document.getElementById("result").innerText =
    "Use Gate B. It has less crowd and faster entry.";
}

function findFood() {
  document.getElementById("result").innerText =
    "Food Stall near Zone 2 has only 5 minutes wait.";
}

function findWashroom() {
  document.getElementById("result").innerText =
    "Washroom in Zone 1 is the nearest and less crowded.";
}

function findExit() {
  document.getElementById("result").innerText =
    "Use Exit Gate C for smoother movement after the match.";
}